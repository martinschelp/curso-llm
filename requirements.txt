# Clase 1 - Practica de LLMs (tokenizacion, embeddings y atencion)
# Versiones fijadas y probadas con Python 3.11 (Windows, CPU).
# Instalar con:  pip install -r requirements.txt

# --- Nucleo cientifico ---
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5
matplotlib==3.11.1
scikit-learn==1.9.0

# --- Deep learning / Transformers ---
torch==2.13.0
transformers==5.14.1
tokenizers==0.22.2
sentencepiece==0.2.2

# --- Embeddings clasicos (Word2Vec / GloVe) ---
gensim==4.4.0

# --- Visualizacion de embeddings y atencion ---
umap-learn==0.5.12
bertviz==1.4.1

# --- Cliente de Ollama (modelos locales, ej: gemma) ---
# Requiere tener Ollama instalado aparte: https://ollama.com
ollama==0.6.2

# --- Soporte Jupyter (para correr el notebook localmente) ---
ipykernel==7.3.0
nbclient==0.11.0

# --- Redes corporativas con proxy que intercepta HTTPS ---
# Evita errores SSLCertVerificationError al descargar modelos de Hugging Face.
pip-system-certs==5.3
